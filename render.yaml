services:
  - type: web
    name: y2k-portfolio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
